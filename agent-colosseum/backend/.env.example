# ---------------------------------------------------------------------------
# Agent Colosseum Configuration
# ---------------------------------------------------------------------------

# --- General ---
# Set to "true" to run without external dependencies (Bedrock, Neo4j, MongoDB)
# In mock mode, the backend simulates agent responses and database queries.
MOCK_MODE=true

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# --- AWS Bedrock (LLM) ---
# Required if MOCK_MODE=false
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=anthropic.claude-sonnet-4-5-20250929-v1:0
# Ensure your AWS credentials are set via `aws configure` or environment variables:
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...

# --- Neo4j (Strategy Graph) ---
# Required if MOCK_MODE=false
# Use Neo4j AuraDB or a local instance
NEO4J_URI=neo4j+s://your-instance.databases.neo4j.io
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-password
# Dimensions for vector embeddings (if using vector search)
NEO4J_VECTOR_DIMENSIONS=1536

# --- MongoDB (Match Archive) ---
# Required if MOCK_MODE=false
# Use MongoDB Atlas or a local instance
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/?retryWrites=true&w=majority

# --- Datadog (Observability) ---
# Optional. If set, LLM Observability and custom metrics will be enabled.
# DD_API_KEY=your-datadog-api-key
# DD_SITE=datadoghq.com
# DD_ENV=dev
# DD_SERVICE=agent-colosseum
